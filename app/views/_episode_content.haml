#current-episode
  %div.current-episode-content
    #episode-title
      %h2
        %a{ :href => "/episodes/#{@episode.id}" }
          = "Episódio #{@episode.id} - #{@episode.title} - #{@episode.duration}"
    #episode-description
      %p
        = @episode.description
    %div.CodeRay
      = show_code(@episode.code)

  = partial :player, :locals => {:episode => @episode}